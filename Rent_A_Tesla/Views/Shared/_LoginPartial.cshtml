@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-info" asp-area="Identity" asp-page="/Account/Manage/Index" style="font-size: 20px" title="Manage">Hello @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-bg-primary">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item me-2">
        <a class="nav-link text-bg-primary" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item me-2">
        <a class="nav-link text-bg-primary" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
